# GEPA lineage v2 — deadline policy on a real timing signal.
# Based on: tb-gepa-twins-v1 (mia-v1 5132351e / mie-v0 a4924e4c).
# Change (ONE, two coupled parts that only exist together):
#   1. timing_signal: true — harness (d403b716) appends
#      "[Trial timing: <N>s total.]" to the task message; N = the task's own
#      agent.timeout_sec x run timeout multiplier.
#   2. Mia: one new paragraph after the verifier-first block — deterministic
#      three-phase budget policy (>60% build / 60-25% converge / <25%
#      emergency: artifact now, cheapest check, DONE). Never start a step
#      that can't finish in the remaining budget; never wait on Mie near
#      the wall.
# Targeting (Wren's tactic #1, msg 0200f616): the timeout-zero family —
#   write-compressor, dna-assembly, gcode-to-text, gpt2-codegolf,
#   make-mips-interpreter, rstan-to-pystan, train-fasttext, torch-*-parallelism.
# Mie unchanged (mie-v0). Provider/config identical to v1.
schema_version: "1"
condition: tb-gepa-twins-v2
timing_signal: true
roster:
  - id: mia
    kind: orchestrator
    role: conn
    count: 1
    endpoint: deepseek-ai/Deepseek-V4-Flash
    model_revision: deepseek/deepseek-v4-flash-20260731
    prompt:
      path: personas/bench/gepa/mia-v2.md
      sha256: cce6b223a0ac651ff762c4f50037b14f41d304571353a1e0797e30fffcb4b94d
    generation:
      thinking_effort: high
  - id: mie
    kind: worker
    role: twin
    count: 1
    endpoint: deepseek-ai/Deepseek-V4-Flash
    model_revision: deepseek/deepseek-v4-flash-20260731
    prompt:
      path: personas/bench/gepa/mie-v0.md
      sha256: a4924e4c4507ac466a7500bb34a04a782618df41dba850b6f0f729787c48439b
    generation:
      thinking_effort: high
prices:
  deepseek-ai/Deepseek-V4-Flash:
    input_per_million_usd: 0.14
    cached_input_per_million_usd: 0.14
    output_per_million_usd: 0.28
    cache_read_rate: 0.0
trial_budget:
  timeout_seconds: 36000
